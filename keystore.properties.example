# Copy to keystore.properties and fill in. That file is gitignored; this one is not, so never
# put real passwords here.
#
# storeFile is resolved from the repository root.
storeFile=gearslip-release.jks
storePassword=
keyAlias=gearslip
keyPassword=
